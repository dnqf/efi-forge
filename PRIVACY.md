# 隐私说明

当前版本只加载仓库中的匿名演示硬件报告，不读取真实电脑硬件，也不发送遥测。

未来原生扫描模块遵循以下约束：

- 硬件分析默认在本机完成。
- 不采集 Windows 用户名、网络密码、产品密钥或磁盘唯一序列号。
- SMBIOS、MLB、ROM 和 SystemUUID 不写入普通日志。
- 诊断包由用户主动导出，并在导出前自动脱敏。
- 任何社区样本提交必须由用户单独确认。

