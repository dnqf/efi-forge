# 隐私说明

EFI Forge 默认不提供账号、云同步、遥测或后台上传。硬件分析、规则匹配、EFI 检查、组件融合和报告生成都在本机完成。

## 本机硬件扫描

用户点击“扫描这台电脑”后，Windows 桌面版会只读查询系统类型、固件模式、CPU、主板/BIOS，以及显卡、网络、音频和存储设备的型号与 PCI 身份。扫描用于生成当前会话中的硬件报告，不会读取 Windows 用户名、网络密码、浏览器数据、产品密钥或磁盘唯一序列号。

导出的脱敏硬件报告可能包含：整机厂商与型号、ThinkPad 四位 Machine Type、CPU 名称/代际/核心数、主板型号、BIOS 版本和日期、设备名称、PCI Vendor/Device/Subsystem/Revision ID、可选父 PCI 身份、设备类别、存储模式线索、控制器列表、笔记本设备存在性以及采集时间。原始 PNP 实例路径只在扫描进程内用于标准化设备身份，不写入报告；Windows v2 扫描优先单独列出控制器，避免为每个子设备反复追溯路径。导出前不会加入 SMBIOS 序列号、MLB、ROM、SystemUUID、Windows 用户路径或网络凭据。报告只有在用户明确点击导出时才会写入文件。

## EFI、ACPI 与用户组件

- 用户选择的完整 EFI、Kext、AML、UEFI Driver 和 USB Map 只在本机读取与校验。
- 工具不会上传这些文件，也不会执行用户或社区 EFI 中的 Windows 程序、脚本、Kext 或 UEFI Driver。
- DSDT 静态分析会在本机记录 ACPI 表标识、布尔线索、文件长度与 SHA-256；不会自动上传或反编译完整表。
- 完整 EFI 校验如果发现已填写的 SMBIOS 身份，只显示通用隐私警告，不显示或复制这些值到日志和验证证据中。
- 融合和复制只在用户明确选择后写入新目录；生成的本地报告可能记录组件名称、版本、哈希、相对目标路径和用户选择的处理动作。

## 网络访问

应用不会为硬件扫描或社区机型匹配联网。daliansky/Hackintosh 机型索引以固定提交的最小离线快照随源码提供，运行时不会自动访问候选仓库。

只有用户在桌面版明确开始“项目候选 EFI/官方组件暂存包”构建时，应用才会访问 `components.lock.json` 中锁定的上游 HTTPS 资产地址，下载 OpenCore、Kext、SSDT 或补丁。下载内容受固定大小和 SHA-256 校验约束。上游托管服务可能按其自身政策记录常规网络元数据，例如 IP 地址、时间和 User-Agent；EFI Forge 不控制这些上游日志。

## SMBIOS 与验证证据

自动候选所需的 SMBIOS 身份由锁定 OpenCore 工具在本机生成并直接写入用户选择的本地构建目录。EFI Forge 不上传这些身份，也不会把真实序列号、MLB、ROM 或 SystemUUID 写入脱敏硬件报告或验证证据。

用户主动导出的验证证据会包含用于防止错配的硬件摘要、BIOS 信息、目标 macOS、OpenCore 版本、`config.plist` SHA-256、验证阶段、逐项结果和用户填写的备注。备注由用户自行控制；请勿在备注中粘贴真实 Apple 身份、账号、个人路径或其他敏感信息。

“转移 / 求助摘要”只包含整机与主板型号、BIOS 版本、CPU 名称、兼容性计数、证据缺口、候选资料、EFI 来源类型、验证级别和 `config.plist` SHA-256。生成器不会写入 EFI 根目录、本机用户名路径、SMBIOS 身份、序列号或网络凭据；用户仍应在公开分享前检查硬件型号是否属于其希望披露的信息。

## 保留、分享与删除

应用没有服务器端账户或云端保留机制。报告、候选 EFI、缓存、融合副本和验证证据均保存在本机。用户决定是否保留或分享这些文件，也可以使用系统文件管理器删除。公开提交问题或样本前，请再次检查文件内容；不要公开未脱敏 EFI、完整 ACPI 转储或个人备注。

若发现意外采集、路径泄漏或身份字段泄漏，请按 [SECURITY.md](SECURITY.md) 使用 GitHub 私密漏洞报告渠道联系维护者。
